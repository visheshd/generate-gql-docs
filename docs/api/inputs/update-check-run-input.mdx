---
id: update-check-run-input
title: UpdateCheckRunInput
---

Autogenerated input type of UpdateCheckRun

```graphql
type UpdateCheckRunInput {
  actions: [CheckRunAction!]
  checkRunId: ID!
  clientMutationId: String
  completedAt: DateTime
  conclusion: CheckConclusionState
  detailsUrl: URI
  externalId: String
  name: String
  output: CheckRunOutput
  repositoryId: ID!
  startedAt: DateTime
  status: RequestableCheckStatusState
}
```

### Fields

#### `actions` ([`[CheckRunAction!]`](/docs/api/inputs/check-run-action))

Possible further actions the integrator can perform, which a user may trigger.

#### `checkRunId` ([`ID!`](/docs/api/scalars/id))

The node of the check.

#### `clientMutationId` ([`String`](/docs/api/scalars/string))

A unique identifier for the client performing the mutation.

#### `completedAt` ([`DateTime`](/docs/api/scalars/date-time))

The time that the check run finished.

#### `conclusion` ([`CheckConclusionState`](/docs/api/enums/check-conclusion-state))

The final conclusion of the check.

#### `detailsUrl` ([`URI`](/docs/api/scalars/uri))

The URL of the integrator's site that has the full details of the check.

#### `externalId` ([`String`](/docs/api/scalars/string))

A reference for the run on the integrator's system.

#### `name` ([`String`](/docs/api/scalars/string))

The name of the check.

#### `output` ([`CheckRunOutput`](/docs/api/inputs/check-run-output))

Descriptive details about the run.

#### `repositoryId` ([`ID!`](/docs/api/scalars/id))

The node ID of the repository.

#### `startedAt` ([`DateTime`](/docs/api/scalars/date-time))

The time that the check run began.

#### `status` ([`RequestableCheckStatusState`](/docs/api/enums/requestable-check-status-state))

The current status.
