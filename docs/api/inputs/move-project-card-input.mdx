---
id: move-project-card-input
title: MoveProjectCardInput
---

Autogenerated input type of MoveProjectCard

```graphql
type MoveProjectCardInput {
  afterCardId: ID
  cardId: ID!
  clientMutationId: String
  columnId: ID!
}
```

### Fields

#### `afterCardId` ([`ID`](/docs/api/scalars/id))

Place the new card after the card with this id. Pass null to place it at the top.

#### `cardId` ([`ID!`](/docs/api/scalars/id))

The id of the card to move.

#### `clientMutationId` ([`String`](/docs/api/scalars/string))

A unique identifier for the client performing the mutation.

#### `columnId` ([`ID!`](/docs/api/scalars/id))

The id of the column to move it into.
