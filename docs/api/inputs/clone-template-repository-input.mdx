---
id: clone-template-repository-input
title: CloneTemplateRepositoryInput
---

Autogenerated input type of CloneTemplateRepository

```graphql
type CloneTemplateRepositoryInput {
  clientMutationId: String
  description: String
  includeAllBranches: Boolean
  name: String!
  ownerId: ID!
  repositoryId: ID!
  visibility: RepositoryVisibility!
}
```

### Fields

#### `clientMutationId` ([`String`](/docs/api/scalars/string))

A unique identifier for the client performing the mutation.

#### `description` ([`String`](/docs/api/scalars/string))

A short description of the new repository.

#### `includeAllBranches` ([`Boolean`](/docs/api/scalars/boolean))

Whether to copy all branches from the template to the new repository. Defaults
to copying only the default branch of the template.

#### `name` ([`String!`](/docs/api/scalars/string))

The name of the new repository.

#### `ownerId` ([`ID!`](/docs/api/scalars/id))

The ID of the owner for the new repository.

#### `repositoryId` ([`ID!`](/docs/api/scalars/id))

The Node ID of the template repository.

#### `visibility` ([`RepositoryVisibility!`](/docs/api/enums/repository-visibility))

Indicates the repository's visibility level.
