---
id: deployment-review-state
title: DeploymentReviewState
---

The possible states for a deployment review.

```graphql
enum DeploymentReviewState {
  APPROVED
  REJECTED
}
```

### Values

#### `APPROVED`

The deployment was approved.

#### `REJECTED`

The deployment was rejected.
