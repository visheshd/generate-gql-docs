---
id: branch-protection-rule-conflict-edge
title: BranchProtectionRuleConflictEdge
---

An edge in a connection.

```graphql
type BranchProtectionRuleConflictEdge {
  cursor: String!
  node: BranchProtectionRuleConflict
}
```

### Fields

#### `cursor` ([`String!`](/docs/api/scalars/string))

A cursor for use in pagination.

#### `node` ([`BranchProtectionRuleConflict`](/docs/api/objects/branch-protection-rule-conflict))

The item at the end of the edge.
