---
id: environment-connection
title: EnvironmentConnection
---

The connection type for Environment.

```graphql
type EnvironmentConnection {
  edges: [EnvironmentEdge]
  nodes: [Environment]
  pageInfo: PageInfo!
  totalCount: Int!
}
```

### Fields

#### `edges` ([`[EnvironmentEdge]`](/docs/api/objects/environment-edge))

A list of edges.

#### `nodes` ([`[Environment]`](/docs/api/objects/environment))

A list of nodes.

#### `pageInfo` ([`PageInfo!`](/docs/api/objects/page-info))

Information to aid in pagination.

#### `totalCount` ([`Int!`](/docs/api/scalars/int))

Identifies the total count of items in the connection.
