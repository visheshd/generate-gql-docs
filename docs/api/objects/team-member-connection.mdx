---
id: team-member-connection
title: TeamMemberConnection
---

The connection type for User.

```graphql
type TeamMemberConnection {
  edges: [TeamMemberEdge]
  nodes: [User]
  pageInfo: PageInfo!
  totalCount: Int!
}
```

### Fields

#### `edges` ([`[TeamMemberEdge]`](/docs/api/objects/team-member-edge))

A list of edges.

#### `nodes` ([`[User]`](/docs/api/objects/user))

A list of nodes.

#### `pageInfo` ([`PageInfo!`](/docs/api/objects/page-info))

Information to aid in pagination.

#### `totalCount` ([`Int!`](/docs/api/scalars/int))

Identifies the total count of items in the connection.
