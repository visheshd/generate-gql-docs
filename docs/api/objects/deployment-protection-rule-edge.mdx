---
id: deployment-protection-rule-edge
title: DeploymentProtectionRuleEdge
---

An edge in a connection.

```graphql
type DeploymentProtectionRuleEdge {
  cursor: String!
  node: DeploymentProtectionRule
}
```

### Fields

#### `cursor` ([`String!`](/docs/api/scalars/string))

A cursor for use in pagination.

#### `node` ([`DeploymentProtectionRule`](/docs/api/objects/deployment-protection-rule))

The item at the end of the edge.
