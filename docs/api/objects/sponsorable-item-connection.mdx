---
id: sponsorable-item-connection
title: SponsorableItemConnection
---

The connection type for SponsorableItem.

```graphql
type SponsorableItemConnection {
  edges: [SponsorableItemEdge]
  nodes: [SponsorableItem]
  pageInfo: PageInfo!
  totalCount: Int!
}
```

### Fields

#### `edges` ([`[SponsorableItemEdge]`](/docs/api/objects/sponsorable-item-edge))

A list of edges.

#### `nodes` ([`[SponsorableItem]`](/docs/api/unions/sponsorable-item))

A list of nodes.

#### `pageInfo` ([`PageInfo!`](/docs/api/objects/page-info))

Information to aid in pagination.

#### `totalCount` ([`Int!`](/docs/api/scalars/int))

Identifies the total count of items in the connection.
