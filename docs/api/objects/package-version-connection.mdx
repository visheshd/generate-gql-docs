---
id: package-version-connection
title: PackageVersionConnection
---

The connection type for PackageVersion.

```graphql
type PackageVersionConnection {
  edges: [PackageVersionEdge]
  nodes: [PackageVersion]
  pageInfo: PageInfo!
  totalCount: Int!
}
```

### Fields

#### `edges` ([`[PackageVersionEdge]`](/docs/api/objects/package-version-edge))

A list of edges.

#### `nodes` ([`[PackageVersion]`](/docs/api/objects/package-version))

A list of nodes.

#### `pageInfo` ([`PageInfo!`](/docs/api/objects/page-info))

Information to aid in pagination.

#### `totalCount` ([`Int!`](/docs/api/scalars/int))

Identifies the total count of items in the connection.
