---
id: pull-request-connection
title: PullRequestConnection
---

The connection type for PullRequest.

```graphql
type PullRequestConnection {
  edges: [PullRequestEdge]
  nodes: [PullRequest]
  pageInfo: PageInfo!
  totalCount: Int!
}
```

### Fields

#### `edges` ([`[PullRequestEdge]`](/docs/api/objects/pull-request-edge))

A list of edges.

#### `nodes` ([`[PullRequest]`](/docs/api/objects/pull-request))

A list of nodes.

#### `pageInfo` ([`PageInfo!`](/docs/api/objects/page-info))

Information to aid in pagination.

#### `totalCount` ([`Int!`](/docs/api/scalars/int))

Identifies the total count of items in the connection.
