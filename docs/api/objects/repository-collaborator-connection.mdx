---
id: repository-collaborator-connection
title: RepositoryCollaboratorConnection
---

The connection type for User.

```graphql
type RepositoryCollaboratorConnection {
  edges: [RepositoryCollaboratorEdge]
  nodes: [User]
  pageInfo: PageInfo!
  totalCount: Int!
}
```

### Fields

#### `edges` ([`[RepositoryCollaboratorEdge]`](/docs/api/objects/repository-collaborator-edge))

A list of edges.

#### `nodes` ([`[User]`](/docs/api/objects/user))

A list of nodes.

#### `pageInfo` ([`PageInfo!`](/docs/api/objects/page-info))

Information to aid in pagination.

#### `totalCount` ([`Int!`](/docs/api/scalars/int))

Identifies the total count of items in the connection.
