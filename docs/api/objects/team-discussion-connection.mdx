---
id: team-discussion-connection
title: TeamDiscussionConnection
---

The connection type for TeamDiscussion.

```graphql
type TeamDiscussionConnection {
  edges: [TeamDiscussionEdge]
  nodes: [TeamDiscussion]
  pageInfo: PageInfo!
  totalCount: Int!
}
```

### Fields

#### `edges` ([`[TeamDiscussionEdge]`](/docs/api/objects/team-discussion-edge))

A list of edges.

#### `nodes` ([`[TeamDiscussion]`](/docs/api/objects/team-discussion))

A list of nodes.

#### `pageInfo` ([`PageInfo!`](/docs/api/objects/page-info))

Information to aid in pagination.

#### `totalCount` ([`Int!`](/docs/api/scalars/int))

Identifies the total count of items in the connection.
