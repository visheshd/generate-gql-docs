---
id: page-info
title: PageInfo
---

Information about pagination in a connection.

```graphql
type PageInfo {
  endCursor: String
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
}
```

### Fields

#### `endCursor` ([`String`](/docs/api/scalars/string))

When paginating forwards, the cursor to continue.

#### `hasNextPage` ([`Boolean!`](/docs/api/scalars/boolean))

When paginating forwards, are there more items?

#### `hasPreviousPage` ([`Boolean!`](/docs/api/scalars/boolean))

When paginating backwards, are there more items?

#### `startCursor` ([`String`](/docs/api/scalars/string))

When paginating backwards, the cursor to continue.
