---
id: commit-history-connection
title: CommitHistoryConnection
---

The connection type for Commit.

```graphql
type CommitHistoryConnection {
  edges: [CommitEdge]
  nodes: [Commit]
  pageInfo: PageInfo!
  totalCount: Int!
}
```

### Fields

#### `edges` ([`[CommitEdge]`](/docs/api/objects/commit-edge))

A list of edges.

#### `nodes` ([`[Commit]`](/docs/api/objects/commit))

A list of nodes.

#### `pageInfo` ([`PageInfo!`](/docs/api/objects/page-info))

Information to aid in pagination.

#### `totalCount` ([`Int!`](/docs/api/scalars/int))

Identifies the total count of items in the connection.
