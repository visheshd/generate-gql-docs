---
id: pull-request
title: PullRequest
---

A repository pull request.

```graphql
type PullRequest implements Assignable, Closable, Comment, Labelable, Lockable, Node, Reactable, RepositoryNode, Subscribable, UniformResourceLocatable, Updatable, UpdatableComment {
  activeLockReason: LockReason
  additions: Int!
  assignees(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
  ): UserConnection!
  author: Actor
  authorAssociation: CommentAuthorAssociation!
  autoMergeRequest: AutoMergeRequest
  baseRef: Ref
  baseRefName: String!
  baseRefOid: GitObjectID!
  baseRepository: Repository
  body: String!
  bodyHTML: HTML!
  bodyText: String!
  changedFiles: Int!
  checksResourcePath: URI!
  checksUrl: URI!
  closed: Boolean!
  closedAt: DateTime
  closingIssuesReferences(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
    orderBy: IssueOrder
  ): IssueConnection
  comments(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
    orderBy: IssueCommentOrder
  ): IssueCommentConnection!
  commits(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
  ): PullRequestCommitConnection!
  createdAt: DateTime!
  createdViaEmail: Boolean!
  databaseId: Int
  deletions: Int!
  editor: Actor
  files(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
  ): PullRequestChangedFileConnection
  headRef: Ref
  headRefName: String!
  headRefOid: GitObjectID!
  headRepository: Repository
  headRepositoryOwner: RepositoryOwner
  hovercard(includeNotificationContexts: Boolean = "true"): Hovercard!
  id: ID!
  includesCreatedEdit: Boolean!
  isCrossRepository: Boolean!
  isDraft: Boolean!
  isReadByViewer: Boolean
  labels(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
    orderBy: LabelOrder = "[object Object]"
  ): LabelConnection
  lastEditedAt: DateTime
  latestOpinionatedReviews(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
    writersOnly: Boolean
  ): PullRequestReviewConnection
  latestReviews(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
  ): PullRequestReviewConnection
  locked: Boolean!
  maintainerCanModify: Boolean!
  mergeCommit: Commit
  mergeable: MergeableState!
  merged: Boolean!
  mergedAt: DateTime
  mergedBy: Actor
  milestone: Milestone
  number: Int!
  participants(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
  ): UserConnection!
  permalink: URI!
  potentialMergeCommit: Commit
  projectCards(
    after: String
    archivedStates: [ProjectCardArchivedState] = [ARCHIVED, NOT_ARCHIVED]
    before: String
    first: Int = 0
    last: Int = 0
  ): ProjectCardConnection!
  publishedAt: DateTime
  reactionGroups: [ReactionGroup!]
  reactions(
    after: String
    before: String
    content: ReactionContent
    first: Int = 0
    last: Int = 0
    orderBy: ReactionOrder
  ): ReactionConnection!
  repository: Repository!
  resourcePath: URI!
  revertResourcePath: URI!
  revertUrl: URI!
  reviewDecision: PullRequestReviewDecision
  reviewRequests(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
  ): ReviewRequestConnection
  reviewThreads(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
  ): PullRequestReviewThreadConnection!
  reviews(
    after: String
    author: String
    before: String
    first: Int = 0
    last: Int = 0
    states: [PullRequestReviewState!] = []
  ): PullRequestReviewConnection
  state: PullRequestState!
  suggestedReviewers: [SuggestedReviewer]!
  timeline(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
    since: DateTime
  ): PullRequestTimelineConnection!
  timelineItems(
    after: String
    before: String
    first: Int = 0
    itemTypes: [PullRequestTimelineItemsItemType!] = []
    last: Int = 0
    since: DateTime
    skip: Int = 0
  ): PullRequestTimelineItemsConnection!
  title: String!
  updatedAt: DateTime!
  url: URI!
  userContentEdits(
    after: String
    before: String
    first: Int = 0
    last: Int = 0
  ): UserContentEditConnection
  viewerCanApplySuggestion: Boolean!
  viewerCanDeleteHeadRef: Boolean!
  viewerCanDisableAutoMerge: Boolean!
  viewerCanEnableAutoMerge: Boolean!
  viewerCanReact: Boolean!
  viewerCanSubscribe: Boolean!
  viewerCanUpdate: Boolean!
  viewerCannotUpdateReasons: [CommentCannotUpdateReason!]!
  viewerDidAuthor: Boolean!
  viewerLatestReview: PullRequestReview
  viewerLatestReviewRequest: ReviewRequest
  viewerMergeBodyText(mergeType: PullRequestMergeMethod): String!
  viewerMergeHeadlineText(mergeType: PullRequestMergeMethod): String!
  viewerSubscription: SubscriptionState
}
```

### Fields

#### `activeLockReason` ([`LockReason`](/docs/api/enums/lock-reason))

Reason that the conversation was locked.

#### `additions` ([`Int!`](/docs/api/scalars/int))

The number of additions in this pull request.

#### `assignees` ([`UserConnection!`](/docs/api/objects/user-connection))

A list of Users assigned to this object.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

#### `author` ([`Actor`](/docs/api/interfaces/actor))

The actor who authored the comment.

#### `authorAssociation` ([`CommentAuthorAssociation!`](/docs/api/enums/comment-author-association))

Author's association with the subject of the comment.

#### `autoMergeRequest` ([`AutoMergeRequest`](/docs/api/objects/auto-merge-request))

Returns the auto-merge request object if one exists for this pull request.

#### `baseRef` ([`Ref`](/docs/api/objects/ref))

Identifies the base Ref associated with the pull request.

#### `baseRefName` ([`String!`](/docs/api/scalars/string))

Identifies the name of the base Ref associated with the pull request, even if the ref has been deleted.

#### `baseRefOid` ([`GitObjectID!`](/docs/api/scalars/git-object-id))

Identifies the oid of the base ref associated with the pull request, even if the ref has been deleted.

#### `baseRepository` ([`Repository`](/docs/api/objects/repository))

The repository associated with this pull request's base Ref.

#### `body` ([`String!`](/docs/api/scalars/string))

The body as Markdown.

#### `bodyHTML` ([`HTML!`](/docs/api/scalars/html))

The body rendered to HTML.

#### `bodyText` ([`String!`](/docs/api/scalars/string))

The body rendered to text.

#### `changedFiles` ([`Int!`](/docs/api/scalars/int))

The number of changed files in this pull request.

#### `checksResourcePath` ([`URI!`](/docs/api/scalars/uri))

The HTTP path for the checks of this pull request.

#### `checksUrl` ([`URI!`](/docs/api/scalars/uri))

The HTTP URL for the checks of this pull request.

#### `closed` ([`Boolean!`](/docs/api/scalars/boolean))

`true` if the pull request is closed

#### `closedAt` ([`DateTime`](/docs/api/scalars/date-time))

Identifies the date and time when the object was closed.

#### `closingIssuesReferences` ([`IssueConnection`](/docs/api/objects/issue-connection))

List of issues that were may be closed by this pull request

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

- ##### `orderBy` ([`IssueOrder`](/docs/api/inputs/issue-order))

Ordering options for issues returned from the connection

#### `comments` ([`IssueCommentConnection!`](/docs/api/objects/issue-comment-connection))

A list of comments associated with the pull request.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

- ##### `orderBy` ([`IssueCommentOrder`](/docs/api/inputs/issue-comment-order))

Ordering options for issue comments returned from the connection.

#### `commits` ([`PullRequestCommitConnection!`](/docs/api/objects/pull-request-commit-connection))

A list of commits present in this pull request's head branch not present in the base branch.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

#### `createdAt` ([`DateTime!`](/docs/api/scalars/date-time))

Identifies the date and time when the object was created.

#### `createdViaEmail` ([`Boolean!`](/docs/api/scalars/boolean))

Check if this comment was created via an email reply.

#### `databaseId` ([`Int`](/docs/api/scalars/int))

Identifies the primary key from the database.

#### `deletions` ([`Int!`](/docs/api/scalars/int))

The number of deletions in this pull request.

#### `editor` ([`Actor`](/docs/api/interfaces/actor))

The actor who edited this pull request's body.

#### `files` ([`PullRequestChangedFileConnection`](/docs/api/objects/pull-request-changed-file-connection))

Lists the files changed within this pull request.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

#### `headRef` ([`Ref`](/docs/api/objects/ref))

Identifies the head Ref associated with the pull request.

#### `headRefName` ([`String!`](/docs/api/scalars/string))

Identifies the name of the head Ref associated with the pull request, even if the ref has been deleted.

#### `headRefOid` ([`GitObjectID!`](/docs/api/scalars/git-object-id))

Identifies the oid of the head ref associated with the pull request, even if the ref has been deleted.

#### `headRepository` ([`Repository`](/docs/api/objects/repository))

The repository associated with this pull request's head Ref.

#### `headRepositoryOwner` ([`RepositoryOwner`](/docs/api/interfaces/repository-owner))

The owner of the repository associated with this pull request's head Ref.

#### `hovercard` ([`Hovercard!`](/docs/api/objects/hovercard))

The hovercard information for this issue

- ##### `includeNotificationContexts` ([`Boolean`](/docs/api/scalars/boolean))

Whether or not to include notification contexts

#### `id` ([`ID!`](/docs/api/scalars/id))

#### `includesCreatedEdit` ([`Boolean!`](/docs/api/scalars/boolean))

Check if this comment was edited and includes an edit with the creation data

#### `isCrossRepository` ([`Boolean!`](/docs/api/scalars/boolean))

The head and base repositories are different.

#### `isDraft` ([`Boolean!`](/docs/api/scalars/boolean))

Identifies if the pull request is a draft.

#### `isReadByViewer` ([`Boolean`](/docs/api/scalars/boolean))

Is this pull request read by the viewer

#### `labels` ([`LabelConnection`](/docs/api/objects/label-connection))

A list of labels associated with the object.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

- ##### `orderBy` ([`LabelOrder`](/docs/api/inputs/label-order))

Ordering options for labels returned from the connection.

#### `lastEditedAt` ([`DateTime`](/docs/api/scalars/date-time))

The moment the editor made the last edit

#### `latestOpinionatedReviews` ([`PullRequestReviewConnection`](/docs/api/objects/pull-request-review-connection))

A list of latest reviews per user associated with the pull request.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

- ##### `writersOnly` ([`Boolean`](/docs/api/scalars/boolean))

Only return reviews from user who have write access to the repository

#### `latestReviews` ([`PullRequestReviewConnection`](/docs/api/objects/pull-request-review-connection))

A list of latest reviews per user associated with the pull request that are not also pending review.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

#### `locked` ([`Boolean!`](/docs/api/scalars/boolean))

`true` if the pull request is locked

#### `maintainerCanModify` ([`Boolean!`](/docs/api/scalars/boolean))

Indicates whether maintainers can modify the pull request.

#### `mergeCommit` ([`Commit`](/docs/api/objects/commit))

The commit that was created when this pull request was merged.

#### `mergeable` ([`MergeableState!`](/docs/api/enums/mergeable-state))

Whether or not the pull request can be merged based on the existence of merge conflicts.

#### `merged` ([`Boolean!`](/docs/api/scalars/boolean))

Whether or not the pull request was merged.

#### `mergedAt` ([`DateTime`](/docs/api/scalars/date-time))

The date and time that the pull request was merged.

#### `mergedBy` ([`Actor`](/docs/api/interfaces/actor))

The actor who merged the pull request.

#### `milestone` ([`Milestone`](/docs/api/objects/milestone))

Identifies the milestone associated with the pull request.

#### `number` ([`Int!`](/docs/api/scalars/int))

Identifies the pull request number.

#### `participants` ([`UserConnection!`](/docs/api/objects/user-connection))

A list of Users that are participating in the Pull Request conversation.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

#### `permalink` ([`URI!`](/docs/api/scalars/uri))

The permalink to the pull request.

#### `potentialMergeCommit` ([`Commit`](/docs/api/objects/commit))

The commit that GitHub automatically generated to test if this pull request
could be merged. This field will not return a value if the pull request is
merged, or if the test merge commit is still being generated. See the
`mergeable` field for more details on the mergeability of the pull request.

#### `projectCards` ([`ProjectCardConnection!`](/docs/api/objects/project-card-connection))

List of project cards associated with this pull request.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `archivedStates` ([`[ProjectCardArchivedState]`](/docs/api/enums/project-card-archived-state))

A list of archived states to filter the cards by

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

#### `publishedAt` ([`DateTime`](/docs/api/scalars/date-time))

Identifies when the comment was published at.

#### `reactionGroups` ([`[ReactionGroup!]`](/docs/api/objects/reaction-group))

A list of reactions grouped by content left on the subject.

#### `reactions` ([`ReactionConnection!`](/docs/api/objects/reaction-connection))

A list of Reactions left on the Issue.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `content` ([`ReactionContent`](/docs/api/enums/reaction-content))

Allows filtering Reactions by emoji.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

- ##### `orderBy` ([`ReactionOrder`](/docs/api/inputs/reaction-order))

Allows specifying the order in which reactions are returned.

#### `repository` ([`Repository!`](/docs/api/objects/repository))

The repository associated with this node.

#### `resourcePath` ([`URI!`](/docs/api/scalars/uri))

The HTTP path for this pull request.

#### `revertResourcePath` ([`URI!`](/docs/api/scalars/uri))

The HTTP path for reverting this pull request.

#### `revertUrl` ([`URI!`](/docs/api/scalars/uri))

The HTTP URL for reverting this pull request.

#### `reviewDecision` ([`PullRequestReviewDecision`](/docs/api/enums/pull-request-review-decision))

The current status of this pull request with respect to code review.

#### `reviewRequests` ([`ReviewRequestConnection`](/docs/api/objects/review-request-connection))

A list of review requests associated with the pull request.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

#### `reviewThreads` ([`PullRequestReviewThreadConnection!`](/docs/api/objects/pull-request-review-thread-connection))

The list of all review threads for this pull request.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

#### `reviews` ([`PullRequestReviewConnection`](/docs/api/objects/pull-request-review-connection))

A list of reviews associated with the pull request.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `author` ([`String`](/docs/api/scalars/string))

Filter by author of the review.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

- ##### `states` ([`[PullRequestReviewState!]`](/docs/api/enums/pull-request-review-state))

A list of states to filter the reviews.

#### `state` ([`PullRequestState!`](/docs/api/enums/pull-request-state))

Identifies the state of the pull request.

#### `suggestedReviewers` ([`[SuggestedReviewer]!`](/docs/api/objects/suggested-reviewer))

A list of reviewer suggestions based on commit history and past review comments.

#### `timeline` ([`PullRequestTimelineConnection!`](/docs/api/objects/pull-request-timeline-connection))

<span class="badge badge--warning">DEPRECATED: `timeline` will be removed Use PullRequest.timelineItems instead. Removal on 2020-10-01 UTC.</span>

A list of events, comments, commits, etc. associated with the pull request.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

- ##### `since` ([`DateTime`](/docs/api/scalars/date-time))

Allows filtering timeline events by a `since` timestamp.

#### `timelineItems` ([`PullRequestTimelineItemsConnection!`](/docs/api/objects/pull-request-timeline-items-connection))

A list of events, comments, commits, etc. associated with the pull request.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `itemTypes` ([`[PullRequestTimelineItemsItemType!]`](/docs/api/enums/pull-request-timeline-items-item-type))

Filter timeline items by type.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

- ##### `since` ([`DateTime`](/docs/api/scalars/date-time))

Filter timeline items by a `since` timestamp.

- ##### `skip` ([`Int`](/docs/api/scalars/int))

Skips the first _n_ elements in the list.

#### `title` ([`String!`](/docs/api/scalars/string))

Identifies the pull request title.

#### `updatedAt` ([`DateTime!`](/docs/api/scalars/date-time))

Identifies the date and time when the object was last updated.

#### `url` ([`URI!`](/docs/api/scalars/uri))

The HTTP URL for this pull request.

#### `userContentEdits` ([`UserContentEditConnection`](/docs/api/objects/user-content-edit-connection))

A list of edits to this content.

- ##### `after` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come after the specified cursor.

- ##### `before` ([`String`](/docs/api/scalars/string))

Returns the elements in the list that come before the specified cursor.

- ##### `first` ([`Int`](/docs/api/scalars/int))

Returns the first _n_ elements from the list.

- ##### `last` ([`Int`](/docs/api/scalars/int))

Returns the last _n_ elements from the list.

#### `viewerCanApplySuggestion` ([`Boolean!`](/docs/api/scalars/boolean))

Whether or not the viewer can apply suggestion.

#### `viewerCanDeleteHeadRef` ([`Boolean!`](/docs/api/scalars/boolean))

Check if the viewer can restore the deleted head ref.

#### `viewerCanDisableAutoMerge` ([`Boolean!`](/docs/api/scalars/boolean))

Whether or not the viewer can disable auto-merge

#### `viewerCanEnableAutoMerge` ([`Boolean!`](/docs/api/scalars/boolean))

Whether or not the viewer can enable auto-merge

#### `viewerCanReact` ([`Boolean!`](/docs/api/scalars/boolean))

Can user react to this subject

#### `viewerCanSubscribe` ([`Boolean!`](/docs/api/scalars/boolean))

Check if the viewer is able to change their subscription status for the repository.

#### `viewerCanUpdate` ([`Boolean!`](/docs/api/scalars/boolean))

Check if the current viewer can update this object.

#### `viewerCannotUpdateReasons` ([`[CommentCannotUpdateReason!]!`](/docs/api/enums/comment-cannot-update-reason))

Reasons why the current viewer can not update this comment.

#### `viewerDidAuthor` ([`Boolean!`](/docs/api/scalars/boolean))

Did the viewer author this comment.

#### `viewerLatestReview` ([`PullRequestReview`](/docs/api/objects/pull-request-review))

The latest review given from the viewer.

#### `viewerLatestReviewRequest` ([`ReviewRequest`](/docs/api/objects/review-request))

The person who has requested the viewer for review on this pull request.

#### `viewerMergeBodyText` ([`String!`](/docs/api/scalars/string))

The merge body text for the viewer and method.

- ##### `mergeType` ([`PullRequestMergeMethod`](/docs/api/enums/pull-request-merge-method))

The merge method for the message.

#### `viewerMergeHeadlineText` ([`String!`](/docs/api/scalars/string))

The merge headline text for the viewer and method.

- ##### `mergeType` ([`PullRequestMergeMethod`](/docs/api/enums/pull-request-merge-method))

The merge method for the message.

#### `viewerSubscription` ([`SubscriptionState`](/docs/api/enums/subscription-state))

Identifies if the viewer is watching, not watching, or ignoring the subscribable entity.

### Interfaces

#### [`Assignable`](/docs/api/interfaces/assignable)

An object that can have users assigned to it.

#### [`Closable`](/docs/api/interfaces/closable)

An object that can be closed

#### [`Comment`](/docs/api/interfaces/comment)

Represents a comment.

#### [`Labelable`](/docs/api/interfaces/labelable)

An object that can have labels assigned to it.

#### [`Lockable`](/docs/api/interfaces/lockable)

An object that can be locked.

#### [`Node`](/docs/api/interfaces/node)

An object with an ID.

#### [`Reactable`](/docs/api/interfaces/reactable)

Represents a subject that can be reacted on.

#### [`RepositoryNode`](/docs/api/interfaces/repository-node)

Represents a object that belongs to a repository.

#### [`Subscribable`](/docs/api/interfaces/subscribable)

Entities that can be subscribed to for web and email notifications.

#### [`UniformResourceLocatable`](/docs/api/interfaces/uniform-resource-locatable)

Represents a type that can be retrieved by a URL.

#### [`Updatable`](/docs/api/interfaces/updatable)

Entities that can be updated.

#### [`UpdatableComment`](/docs/api/interfaces/updatable-comment)

Comments that can be updated.
