---
id: topic-audit-entry-data
title: TopicAuditEntryData
---

Metadata for an audit entry with a topic.

```graphql
interface TopicAuditEntryData {
  topic: Topic
  topicName: String
}
```

### Fields

#### `topic` ([`Topic`](/docs/api/objects/topic))

The name of the topic added to the repository

#### `topicName` ([`String`](/docs/api/scalars/string))

The name of the topic added to the repository
